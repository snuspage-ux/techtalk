// Site-wide configuration flags.

export const config = {
  // When true, comments submitted to /api/comments are published immediately.
  // When false, new comments are stored as pending (approved=false) and held
  // for manual moderation before they appear in the public listing.
  autoApproveRequests: true,
};
